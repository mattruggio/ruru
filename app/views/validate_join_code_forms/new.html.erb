<% content_for(:title, "Join Season") %>

<h2>Find Season</h2>

<%= form_with model: @validate_join_code_form do |f| %>
  <%= render "shared/errors", model: @validate_join_code_form %>

  <div>
    <%= f.label :join_code %><br>
    <%= f.text_field :join_code %>
  </div>

  <div>
    <%= f.submit "Find" %>
  </div>
<% end %>